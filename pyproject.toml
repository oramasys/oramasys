[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oramasys"
version = "2.0.0-alpha.1"
description = "Hardware-aware multi-agent orchestration system (oramasys graph + API)"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "oramasys-telos @ git+https://github.com/oramasys/telos.git@9d23b21790c0d7b4f669a7369275aae4008b65da",
    "perpetua-core @ git+https://github.com/oramasys/perpetua-core.git@68420bae21449d65e7dbb079a6b5ae9f80bd8068",
    "fastapi>=0.110",
    "pydantic>=2,<3",
    "uvicorn[standard]>=0.29",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=6.0",
    "httpx",
    "respx>=0.20",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["src/tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/orama"]

[tool.hatch.metadata]
# Required for the immutable git+https direct references above: hatchling
# refuses direct-reference dependencies in [project.dependencies] otherwise.
allow-direct-references = true
