[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oramasys"
version = "2.0.0-alpha.1"
description = "Hardware-aware multi-agent orchestration system (oramasys graph + API)"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "oramasys-telos @ git+https://github.com/oramasys/telos.git@b2143083044496d93be3ca2dc88c28135e034d9f",
    "perpetua-core @ git+https://github.com/oramasys/perpetua-core.git@717f97565583fefd34f4a43064da870c0a95fb9a",
    "fastapi>=0.110",
    "pydantic>=2,<3",
    "uvicorn[standard]>=0.29",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=6.0",
    "httpx",
    "respx>=0.20",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["src/tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/orama"]

[tool.hatch.metadata]
# Required for the immutable git+https direct references above: hatchling
# refuses direct-reference dependencies in [project.dependencies] otherwise.
allow-direct-references = true
